
pipeline {
    agent { label 'stapp01' }

    stages {

        stage('Deploy') {
            steps {
                dir('/var/www/html') {
                    sh '''
                        git pull origin master
                    '''
                }
            }
        }

        stage('Test') {
            steps {
                sh '''
                    curl -f http://stlb01:8091
                '''
            }
        }
    }
}
